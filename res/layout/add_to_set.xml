<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#ffffff" >
    
    <RelativeLayout
        android:id="@+id/head_add_to_meal"
        style="@style/titlebar"
        android:background="#ef5b51" >

        <TextView
            style="@style/titletext"
            android:layout_centerInParent="true"
            android:text="选择加入的套餐"
            android:textColor="#fff" />
    </RelativeLayout>
    
    <Button
        android:id="@+id/dialog_add_to_meal_sure"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="16dp"
        android:background="@drawable/red_background"
        android:text="确认添加"
        android:textColor="#fff"
        android:textSize="18sp"
        android:clickable="false"/>
    
    <ListView 
        android:id="@+id/dialog_add_to_meal"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:layout_below="@id/head_add_to_meal"
        android:layout_above="@id/dialog_add_to_meal_sure"/>
    

</RelativeLayout>
